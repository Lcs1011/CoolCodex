
```
CoolOperation 生成守则：

1. **必须每次**生成 CoolOperation 前查阅当前仓库现状，尤其是目标文件的当前内容和实时行号。必须根据当前仓库现状生成所有文件修改指令需要的行号。**严禁根据记忆、历史行号、上一次对话内容或推测生成行号。**
    
2. 修改文件的指令必须基于目标文件“修改前”的当前真实行号生成，并且在同一个文件内，修改命令应尽量按行号从小到大排列，便于人工审查。切记：底层工具已经处理修改顺序和偏移问题，生成指令时不需要、也不允许根据上文修改重新计算下文行号偏移。
    
3. 如果我只发送 `1`，就代表上一轮执行成功，请继续当前任务，不要重新解释已经完成的步骤。
   
4. 使用 `ReplaceLineRange` 替换代码块时，必须确保替换范围覆盖完整语法结构，不能只替换函数调用、match 分支、结构体、impl 块的一半。对于函数、match 分支、结构体、impl 块等多行结构，必须检查起始行和结束行是否形成完整闭合范围，避免留下半截旧代码或半截参数列表。
   
```



不提交的时候 不修改md

```
CoolOperation 生成守则：

能用提供的文件修改工具 就不要使用命令行  尽可能避免使用命令行


1. **必须每次**生成 CoolOperation 前查阅当前仓库现状，尤其是目标文件的当前内容和实时行号。必须根据当前仓库现状生成所有文件修改指令需要的行号。**严禁根据记忆、历史行号、上一次对话内容或推测生成行号。**
    
2. 修改文件的指令必须基于目标文件“修改前”的当前真实行号生成，并且在同一个文件内，修改命令应尽量按行号从小到大排列，便于人工审查。切记：底层工具已经处理修改顺序和偏移问题，生成指令时不需要、也不允许根据上文修改重新计算下文行号偏移。
    
3. **行号来源必须校验。**生成 CoolOperation 的文件修改行号时，必须确认所使用的行号是目标文件的真实内容行号，而不是搜索工具、GitHub 工具、引用系统、日志系统或其他外部工具包装后的显示行号。若工具返回内容前包含 `encoding`、`sha`、`content`、metadata 等包装行，这些包装行不得计入文件真实行号。生成 `ReplaceLineRange` / `DeleteLine` 等行号指令前，必须换算为目标文件真实行号，并确认 `end_line` 不超过目标文件真实 `line_count`。
    
4. 使用 GitHub `fetch_file` 等工具查看文件时，如果返回结果前两行是 `encoding`、`sha` 等 metadata，第三行才开始显示 `content`，则引用行号不是文件真实行号。此时文件真实行号 = GitHub 显示行号 - metadata 行数。严禁直接把 GitHub 引用行号 `[Lx]` 当作 CoolOperation 的 `start_line` / `end_line` 使用。
    
5. 使用 `ReplaceLineRange` 替换代码块时，必须确保替换范围覆盖完整语法结构，不能只替换函数调用、match 分支、结构体、impl 块的一半。对于函数、match 分支、结构体、impl 块等多行结构，必须检查起始行和结束行是否形成完整闭合范围，避免留下半截旧代码或半截参数列表。
    
6. 如果我只发送 `1`，就代表上一轮执行成功，请继续当前任务，不要重新解释已经完成的步骤。
   
   7 在修改代码的时候，必须要严格检查是否遗漏末尾的。必须检查新增或替换的代码块是否包含完整的 `(` / `)`、`{` / `}`、`[` / `]`、`"` / `'` / `` ` ``、`,`、`;`、`</tag>`。
   关于末尾的这些符号是非常容易出错的，一定要高度警惕。
```


你什么意思？你的意思是你跨次数改东西了？ 那以后就必须要加一个强力的提示词。 就是每次执行必须要基于当前仓库中的行号，不可以基于印象中的行号。 加上这个提示词，能解决这个问题吗？



使用 CoolOperation 的时候切记，所有对文件的修改指令，必须查阅当前仓库，根据当前文件的实时行数来确定指令。禁止基于记忆。



## CoolOperation使用纪律
**每次**生成 CoolOperation 时 再发出前，必须 **二次核对** 以下内容：

绝对不能 污染windows 还有 conda base的 python环境

修改文件的指令要基于当前的行号自上而下进行指令输出。但切记，不需要你计算上文修改对下文造成的偏移影响，执行CoolOperation的工具已经优化。

**必须每次**生成CoolOperation时 都要看一次 仓库的现状，
必须根据当前仓库现状 生成 指令需要的行数。 **严禁**根据记忆

再对代码进行删除或替换时 **强制检查**前后两道三行的代码，是否有遗漏。尤其防止遗漏括号

必须推演是否正确

如果出现问题有详细日志

，一定要留意**开头和结尾** 是否有漏掉内容。 尤其是有各种括号 （比如`{` / `}` ） 的情况 





如果我发 1 就代表执行成功 ，继续我们当前任务





只要 Rust parser 报 delimiter 错误 → 立即停止自动 patch


所有 ReplaceLineRange / DeleteLine / InsertContent 的行号，
必须基于当前仓库当前文件的实时内容，禁止基于记忆

、上一轮输出、旧报错、旧文件片段推断行号。每次生成 CoolOperation 前，必须重新读取或要求用户提供目标文件的当前内容/当前行号。若无法确认当前行号，不得生成行号型修改指令，必须改用整文件替换、整函数锚点替换，或先要求用户贴当前文件片段。


行号型修改必须带 expected_old_content 或 expected_hash。  
执行前 CoolBot 先校验目标行范围当前内容是否等于 expected_old_content。  
不一致就拒绝执行，不允许“差不多就改”。